import { expect } from 'chai';

import ButtonShowHide from '../src/ButtonShowHide';

describe('ButtonShowHide', () => {
  it('should there be a method', () => {

    expect(ButtonShowHide).to.be.exist;
  });
});
